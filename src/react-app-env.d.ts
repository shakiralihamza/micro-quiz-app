/// <reference types="react-scripts" />

export enum Menu {
    Settings = 'Settings',
    Quiz = 'Quiz',
    Result = 'Result'
}

export enum Difficulty {
    Easy = 'Easy',
    Medium = 'Medium',
    Hard = 'Hard'
}